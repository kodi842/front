import React from 'react';
import './HeroSection.css';

const HeroSection = () => (
  <section className="hero">
    <h1>ИИ и мощь — теперь вместе</h1>
    <p>Будущее на колесах</p>
    <button>Узнать больше</button>
  </section>
);

export default HeroSection;